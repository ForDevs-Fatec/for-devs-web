import { styled } from 'styled-components'

export const SectionWrapperGrid_1 = styled.section`
    grid-area: 1 / 1 / 2 / 2;

    padding: 1rem;

    border-radius: 0.5rem;
    background-color: ${({theme}) => theme.colors.black_700};
    box-shadow: 0 0 0.5rem ${({theme}) => theme.colors.black_700};
`

export const SectionWrapperGrid_2 = styled.section`
    grid-area: 1 / 2 / 2 / 3;

    padding: 1rem;

    border-radius: 0.5rem;
    background-color: ${({theme}) => theme.colors.black_700};
    box-shadow: 0 0 0.5rem ${({theme}) => theme.colors.black_700};
`

export const SectionWrapperGrid_3 = styled.section`
    grid-area: 1 / 3 / 2 / 4;

    padding: 1rem;

    border-radius: 0.5rem;
    background-color: ${({theme}) => theme.colors.black_700};
    box-shadow: 0 0 0.5rem ${({theme}) => theme.colors.black_700};
`

export const SectionWrapperGrid_4 = styled.section`
    grid-area: 1 / 4 / 2 / 5;

    padding: 1rem;

    border-radius: 0.5rem;
    background-color: ${({theme}) => theme.colors.black_700};
    box-shadow: 0 0 0.5rem ${({theme}) => theme.colors.black_700};
`

export const SectionWrapperGrid_5 = styled.section`
    grid-area: 2 / 1 / 3 / 3;

    padding: 1rem;

    border-radius: 0.5rem;
    background-color: ${({theme}) => theme.colors.black_700};
    box-shadow: 0 0 0.5rem ${({theme}) => theme.colors.black_700};
`

export const SectionWrapperGrid_6 = styled.section`
    grid-area: 2 / 3 / 3 / 5;

    padding: 1rem;

    border-radius: 0.5rem;
    background-color: ${({theme}) => theme.colors.black_700};
    box-shadow: 0 0 0.5rem ${({theme}) => theme.colors.black_700};
`

export const SectionWrapperGrid_7 = styled.section`
    grid-area: 3 / 1 / 4 / 2;

    padding: 1rem;

    border-radius: 0.5rem;
    background-color: ${({theme}) => theme.colors.black_700};
    box-shadow: 0 0 0.5rem ${({theme}) => theme.colors.black_700};
`

export const SectionWrapperGrid_8 = styled.section`
    grid-area: 3 / 2 / 4 / 3;

    padding: 1rem;

    border-radius: 0.5rem;
    background-color: ${({theme}) => theme.colors.black_700};
    box-shadow: 0 0 0.5rem ${({theme}) => theme.colors.black_700}; 
`

export const SectionWrapperGrid_9 = styled.section`
    grid-area: 3 / 3 / 4 / 5;

    padding: 1rem;

    border-radius: 0.5rem;
    background-color: ${({theme}) => theme.colors.black_700};
    box-shadow: 0 0 0.5rem ${({theme}) => theme.colors.black_700};
`